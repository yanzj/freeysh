### ============================================================================
决(UN) 先(TFQ),垂(tga),段(WDM),予(CBJ),未(FII),尾(NTF)

盛(dnnl),兆(iqv),鼠(vnu),改(NTY)

末(GS),生(TG),隐(BQ),藏(ADNT),羊(udj)

成(DN)--革(af),卸(rhb),剩(tuxj),文(YYGY)

单(UJFJ),制(RMHJ),造(TFKP)
      

### ===========================================================================
派(ire),丫(uhk),典(MAW),夕(qtny),养(udyj)
加(lk)/减(udg)/乘(乘)/除(bwty),甲(lhnh)-乙(nnl)
一(GGLL),丨(HHLL),丿(TTLL),丶(YYLL),乙(NNLL) 

### ========================================================== debian_五笔输入法

1.显示应用程序--->输入法--->输入法配置--->fcitx   启用小企鹅输入法(Fcitx)!

2.在用户设置中 点击  fctix选项  ，并点击确定。

3.根据输入法配置中的提示，打开终端，输入指令（根据自身要求选择）
___________________________________________________________
(这里有三种拼音输入法：fcitx-sunpinyin ，fcitx-googlepinyin 和 fcitx-pinyin ，不需要的可以删掉)
        % sudo apt-get install fcitx-sunpinyin fcitx-googlepinyin fcitx-pinyin
___________________________________________________________
(两种五笔输入法：fcitx-table-wubi 和fcitx-table-wbpy)
        % sudo apt-get fcitx-table-wubi fcitx-table-wbpy
___________________________________________________________
(繁体中文输入，只有一种)
        % sudo apt-get install fcitx-table-cangjie

4.安装通用的输入法码表: fcitx-table* 套件（必装！）
        % sudo apt-get install fcitx-table*

5.应用程序支持（必装*！）
(fcitx-frontend-gtk2 和 fcitx-frontend-gtk3 必装，同时  fcitx-frontend-qt4  也建议一起装上)
        % sudo apt-get install fcitx-frontend-gtk2 fcitx-frontend-gtk3 fcitx-frontend-qt4


6.最后重启，根据自己的快捷键启动输入法（默认    Ctrl+空格   ）
在右下脚会有小键盘，右键 -->配置    ======可以选择各种输入选项


